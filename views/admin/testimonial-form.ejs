<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= testimonial._id ? 'Edit' : 'Add' %> Testimonial - Dignity Kitchen</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .form-container {
            background: white;
            border-radius: 10px;
            box-shadow: var(--shadow);
            padding: 30px;
            margin-top: 20px;
        }
        
        .form-group {
            margin-bottom: 25px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: var(--dark-color);
        }
        
        .form-control {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #e1e5eb;
            border-radius: 5px;
            font-size: 15px;
            color: var(--text-color);
            transition: var(--transition);
        }
        
        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(var(--primary-rgb), 0.1);
            outline: none;
        }
        
        textarea.form-control {
            min-height: 120px;
            resize: vertical;
        }
        
        .rating-select {
            display: flex;
            gap: 10px;
        }
        
        .rating-option {
            display: flex;
            align-items: center;
            gap: 5px;
            cursor: pointer;
        }
        
        .rating-option input {
            margin: 0;
        }
        
        .rating-option .stars {
            color: #FFD700;
        }
        
        .checkbox-container {
            display: flex;
            align-items: center;
            position: relative;
            padding-left: 35px;
            cursor: pointer;
            font-weight: 500;
            color: var(--dark-color);
        }
        
        .checkbox-container input {
            position: absolute;
            opacity: 0;
            cursor: pointer;
            height: 0;
            width: 0;
        }
        
        .checkmark {
            position: absolute;
            top: 0;
            left: 0;
            height: 22px;
            width: 22px;
            background-color: #fff;
            border: 2px solid #e1e5eb;
            border-radius: 4px;
            transition: var(--transition);
        }
        
        .checkbox-container:hover input ~ .checkmark {
            border-color: var(--secondary-color);
        }
        
        .checkbox-container input:checked ~ .checkmark {
            background-color: var(--secondary-color);
            border-color: var(--secondary-color);
        }
        
        .checkmark:after {
            content: "";
            position: absolute;
            display: none;
        }
        
        .checkbox-container input:checked ~ .checkmark:after {
            display: block;
        }
        
        .checkbox-container .checkmark:after {
            left: 7px;
            top: 3px;
            width: 5px;
            height: 10px;
            border: solid white;
            border-width: 0 2px 2px 0;
            transform: rotate(45deg);
        }
        
        .form-text {
            font-size: 13px;
            color: #6c757d;
            margin-top: 5px;
        }
        
        .image-preview {
            margin-top: 15px;
            max-width: 150px;
            border-radius: 5px;
            overflow: hidden;
        }
        
        .image-preview img {
            width: 100%;
            height: auto;
            display: block;
        }
        
        .form-actions {
            display: flex;
            gap: 15px;
            margin-top: 30px;
        }
        
        .btn-cancel {
            background-color: #f8f9fa;
            color: var(--text-color);
            border: 1px solid #e1e5eb;
        }
        
        .btn-cancel:hover {
            background-color: #e9ecef;
        }
    </style>
</head>
<body>
    <div class="admin-container">
        <aside class="sidebar">
            <div class="logo">
                <img src="/images/logo.png" alt="Dignity Kitchen Logo">
                <h2>Admin Panel</h2>
            </div>
            <nav class="admin-nav">
                <ul>
                    <li><a href="/admin"><i class="fas fa-tachometer-alt"></i> Dashboard</a></li>
                    <li><a href="/admin/gallery"><i class="fas fa-images"></i> Gallery</a></li>
                    <li><a href="/admin/products"><i class="fas fa-box"></i> Products</a></li>
                    <li class="active"><a href="/admin/testimonials"><i class="fas fa-quote-right"></i> Testimonials</a></li>
                    <li><a href="/admin/contacts"><i class="fas fa-envelope"></i> Contact Messages</a></li>
                    <li><a href="/admin/settings"><i class="fas fa-cog"></i> Settings</a></li>
                    <li><a href="/admin/logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
                </ul>
            </nav>
        </aside>
        
        <main class="content">
            <header class="admin-header">
                <h1><%= testimonial._id ? 'Edit' : 'Add New' %> Testimonial</h1>
                <div class="user-info">
                    <span>Welcome, Admin</span>
                </div>
            </header>
            
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="message error-message">
                    <i class="fas fa-exclamation-circle"></i>
                    <%= error %>
                </div>
            <% } %>
            
            <div class="form-container">
                <form action="<%= formAction %>" method="POST" enctype="multipart/form-data">
                    <div class="form-group">
                        <label for="name">Client Name *</label>
                        <input type="text" id="name" name="name" class="form-control" value="<%= testimonial.name || '' %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="position">Position *</label>
                        <input type="text" id="position" name="position" class="form-control" value="<%= testimonial.position || '' %>" required>
                        <small class="form-text">E.g., Executive Chef, Restaurant Owner, etc.</small>
                    </div>
                    
                    <div class="form-group">
                        <label for="company">Company/Business (Optional)</label>
                        <input type="text" id="company" name="company" class="form-control" value="<%= testimonial.company || '' %>">
                    </div>
                    
                    <div class="form-group">
                        <label for="content">Testimonial Content *</label>
                        <textarea id="content" name="content" class="form-control" required><%= testimonial.content || '' %></textarea>
                    </div>
                    
                    <div class="form-group">
                        <label>Rating *</label>
                        <div class="rating-select">
                            <% for (let i = 1; i <= 5; i++) { %>
                                <label class="rating-option">
                                    <input type="radio" name="rating" value="<%= i %>" <%= (testimonial.rating || 5) == i ? 'checked' : '' %>>
                                    <span class="stars">
                                        <% for (let j = 1; j <= 5; j++) { %>
                                            <i class="<%= j <= i ? 'fas' : 'far' %> fa-star"></i>
                                        <% } %>
                                    </span>
                                </label>
                            <% } %>
                        </div>
                    </div>
                    
                    <div class="form-group">
                        <label for="image">Client Photo (Optional)</label>
                        <input type="file" id="image" name="image" class="form-control" accept="image/*">
                        <small class="form-text">Recommended size: 200x200 pixels. Square images work best.</small>
                        
                        <% if (testimonial.imageUrl) { %>
                            <div class="image-preview">
                                <img src="<%= testimonial.imageUrl %>" alt="Current client photo">
                                <small class="form-text">Current photo. Upload a new one to replace it.</small>
                            </div>
                        <% } %>
                    </div>
                    
                    <div class="form-group">
                        <label class="checkbox-container">
                            <input type="checkbox" id="featured" name="featured" <%= testimonial.featured ? 'checked' : '' %>>
                            <span class="checkmark"></span>
                            Feature this testimonial on homepage
                        </label>
                        <small class="form-text">Featured testimonials will be displayed in the homepage testimonials section</small>
                    </div>
                    
                    <div class="form-actions">
                        <button type="submit" class="btn primary-btn">
                            <i class="fas fa-save"></i> <%= testimonial._id ? 'Update' : 'Save' %> Testimonial
                        </button>
                        <a href="/admin/testimonials" class="btn btn-cancel">
                            <i class="fas fa-times"></i> Cancel
                        </a>
                    </div>
                </form>
            </div>
        </main>
    </div>

    <script src="/js/admin.js"></script>
    <script>
        // Preview image before upload
        document.getElementById('image').addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(event) {
                    let preview = document.querySelector('.image-preview');
                    if (!preview) {
                        preview = document.createElement('div');
                        preview.className = 'image-preview';
                        const img = document.createElement('img');
                        preview.appendChild(img);
                        const small = document.createElement('small');
                        small.className = 'form-text';
                        small.textContent = 'Image preview';
                        preview.appendChild(small);
                        e.target.parentNode.appendChild(preview);
                    }
                    preview.querySelector('img').src = event.target.result;
                }
                reader.readAsDataURL(file);
            }
        });
    </script>
</body>
</html>
